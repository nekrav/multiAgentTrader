# Options worker

Deterministic options analytics worker for underlying context, IV, skew/smile, vol term structure, Greeks risk, strategy families, and consensus outputs.

Run once locally:

```bash
RUN_ONCE=true python -m src.worker
```
