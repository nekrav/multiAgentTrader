# Futures worker

Deterministic futures analytics worker for directional, curve, flow/open-interest, event/supply, and consensus outputs.

Run once locally:

```bash
RUN_ONCE=true python -m src.worker
```
